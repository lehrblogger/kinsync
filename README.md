Goal: Generate calendar files from Four Seasons Resort itineraries.

Project Plan:
 - Reverse-engineer the FS website API for trip itineraries.
 - Generate .ics files from the JSON.
 - Store the .ics files in a git repository so that it's easy to audit changes. (Remember not to delete events for past trips!)
 - Use the .ics files on a Radicale server so that I can view them alongside my other calends on macOS and iOS.

TODO:
 - send me an email with an ics file of past Four Seasons events, so not kept in git forever.
